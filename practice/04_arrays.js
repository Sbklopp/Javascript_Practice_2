
// TEST 1
// Create a function that takes an array(list) as an argument and
// returns the first element of that array
// Ex: first([3, 5, 9]) returns 3
const first = (arr) => {
    
};

// TEST 2
// Create a function that takes an array as an argument and returns
// the number (length) of elements in the array
// Ex: howMany([3, 5, 3, 10]) returns 4
const howMany = (arr) => {
    
};


// TEST 3
// Create a function that takes an array as an argument and returns
// the last element of that array
// Ex: last([3, 5, 9]) returns 9
const last = (arr) => {
    
};

// TEST 4 
// Create a function that takes an array of numbers as an argument and
// returns the sum of all the elements added together
// Ex: sum([2, 5, 5, 4]) returns 16
// *HINT* use a forEach loop...
const sum = (arr) => {
    
};

// TEST 5
// Create a function that takes an array of numbers as an argument and 
// returns the largest number
// Ex: largest([5, 19, 4, -1]) returns 19
// *hint* use a forEach loop
const largest = (arr) => {
   
}; 

// TEST 6
// Write a function that takes an array of numbers
// and returns the array with 10 added to each element
// Ex: addTen([5, 9, 3, 0]) returns [15, 19, 13, 10]
// *NOTE* try using the "javascript array map" method to solve this problem
const addTen = (arr) => {
   
}


// TEST 7
// Write a function that takes an array of numbers as an argument
// and returns only the elements of the array that are greater than 59
// Ex: passingGrades([95, 78, 54, 83, 49]) returns [95, 78, 83]
// *HINT* use the javascript filter method -- you can do this in a single line
const passingGrades = (arr) => {
    
}

// TEST 8
// Write a function that takes an array of numbers as an argument
// the function should add 10 to each element of the array that is less than 60
// and return the other elements as they are
// Ex: curve([45, 90, 59, 62]) returns [55, 90, 69, 62]
// ** As a challenge try to do this in one line using map and the ternary operator **
const curve = (arr) => {
    
}

// TEST 9
// Write a function that takes an array of numbers as an argument and
// returns the average of all elements rounded up to the nearest whole number
// Ex: average([20, 14, 11]) returns 15
const average = (arr) => {
    
}




module.exports = {
    first,
    howMany,
    last,
    sum,
    largest,
    addTen,
    passingGrades,
    curve,
    average
}